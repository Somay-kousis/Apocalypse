# HANDOFF - update at every sign-off (India / China / US = near-24h coverage)

Three lines max per entry: what's GREEN, what's BROKEN, what's NEXT.
Each owner edits only their own controls/<name>/ dir. Agree on control_spec.yaml before writing checks.

---
## 2026-09-12 (scaffold pass, Somay)
- GREEN: repo skeleton coherent; control_spec.yaml is canonical; incident_public_record.json is ground truth;
  run_checks + replay_attack run OFFLINE; rules 4 & 5 implemented and pass on fixed / fail on broken.
- BROKEN: rules 1,2,3,6,7,8,9 checkers are stubs (SKIP); docker-compose services still TODO; report is outline only.
- NEXT: Somay -> rule 9 (Biscuit replay). Vikas -> rules 2,6,7 checkers + report lead. Junyi -> rules 3,8 + threat model.

## <date> (<name>)
- GREEN:
- BROKEN:
- NEXT:
