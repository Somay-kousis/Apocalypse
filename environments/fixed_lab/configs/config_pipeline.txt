# Hardened config pipeline.
# Preferred: parse untrusted dataset config as data, never template it.
config = yaml.safe_load(user_config)
# If templating is ever unavoidable, ONLY a SandboxedEnvironment with no builtins:
from jinja2.sandbox import SandboxedEnvironment
env = SandboxedEnvironment()
