# Fixed Lab Configurations

Same per-boundary layout as `broken_lab/configs/`, one subfolder per rule,
but hardened so all 9 containment rules pass. Diff any `bN_*` folder against
its `broken_lab` counterpart to see exactly what one rule changes.
