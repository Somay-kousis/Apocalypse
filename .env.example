# The CORE suite is fully offline and needs NO keys:
#   python -m validation.run_checks   --target environments/broken_lab/configs
#   python -m validation.replay_attack --target environments/broken_lab/configs
#
# Keys below are ONLY for the optional stretch goal (validation/adaptive_agent.py).
# Do not put anything that needs these on the critical path.

ANTHROPIC_API_KEY=            # optional: adaptive-agent stretch goal only
